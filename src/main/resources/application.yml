server:
  port: 8081

spring:
  datasource:
    url: jdbc:h2:mem:hello

  h2:
    console:
      enabled: true

  liquibase:
    enabled: true
    change-log: classpath:db/changelog/db.changelog-master.xml

  jpa:
    generate-ddl: false